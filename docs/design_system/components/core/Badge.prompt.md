Small pill for categories, counts, and inline status tags.

```jsx
<Badge tone="completed" dot>Completed</Badge>
<Badge tone="signal">Health</Badge>
<Badge tone="neutral" mono>28d</Badge>
```

Tones map to the status family + `neutral`/`signal`. For full occurrence-row status use the dedicated `StatusPill`.
