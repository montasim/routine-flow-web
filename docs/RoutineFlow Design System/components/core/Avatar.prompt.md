User avatar for the profile menu and settings header.

```jsx
<Avatar name="Ayaan Rahman" size={40} />
<Avatar name="Ayaan" src="/me.jpg" />
```

Falls back to colored initials (deterministic hue from the name) when `src` is absent.
